Настройка:

composer install - установка зависимостей
vendor\bin\doctrine orm:schema-tool:create - создание сущности Product в БД <db_name>


Файл конфигурации testing.ini:

db_serv = адрес сервера БД
db_name = имя БД
db_user = имя пользователя БД
db_pass = пароль пользователя БД

smtp_host = smtp хост для отправки почты
mail_user = имя пользователя почты
mail_pass = пароль пользователя почты
mail_dest = адрес, на который надо отправлять результаты работы
mail_from = адрес отправителя (подставляется в письмо в поле From)
mail_name = имя отправителя (подставляется в письмо в поле From)
